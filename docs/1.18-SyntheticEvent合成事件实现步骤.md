# React SyntheticEvent 合成事件实现

## 第一部分:顶层鸟瞰

（待补充）

---

## 第二部分:实战步骤

### 步骤 1:新建 SyntheticEvent.ts，实现事件系统核心逻辑

**路径:** `packages/react-dom/src/SyntheticEvent.ts`

**修改内容:**
1. 定义 `DOMElement` 接口，在 DOM 节点上挂载 props 缓存键 `__props`
2. 实现 `updateFiberProps`，将 Fiber 的 props 保存到 DOM 节点上
3. 实现 `initEvent`，在容器上注册原生事件监听（当前支持 `click`）
4. 实现 `dispatchEvent`，作为统一的事件派发入口
5. 实现 `collectPaths`，从 target 向上遍历收集捕获/冒泡回调链
6. 实现 `createSyntheticEvent`，包装原生事件并重写 `stopPropagation`
7. 实现 `triggerEventFlow`，按顺序执行捕获/冒泡回调，遇到 `__stopPropagation` 中断

```diff
+ import { Props } from 'shared/ReactTypes';
+ import { Container } from 'hostConfig';
+
+ export interface DOMElement extends Element {
+   [elementPropsKey]: Props;
+ }
+
+ type EventCallback = (e: Event) => void;
+
+ interface Paths {
+   capture: EventCallback[];
+   bubble: EventCallback[];
+ }
+
+ interface SyntheticEvent extends Event {
+   __stopPropagation: boolean;
+ }
+
+ export const elementPropsKey = '__props';
+ const validEventTypeList = ['click'];
+
+ export function updateFiberProps(node: DOMElement, props: Props) {
+   node[elementPropsKey] = props;
+ }
+
+ export function initEvent(container: Container, eventType: string) {
+   if (!validEventTypeList.includes(eventType)) {
+     console.warn('initEvent 未实现的事件类型', eventType);
+     return;
+   }
+   container.addEventListener(eventType, (e: Event) => {
+     dispatchEvent(container, eventType, e);
+   });
+ }
+
+ function dispatchEvent(container: Container, eventType: string, e: Event) {
+   const targetElement = e.target;
+   if (targetElement == null) {
+     console.warn('事件不存在targetElement', e);
+     return;
+   }
+   const { bubble, capture } = collectPaths(
+     targetElement as DOMElement,
+     container,
+     eventType
+   );
+   const syntheticEvent = createSyntheticEvent(e);
+   triggerEventFlow(capture, syntheticEvent);
+   if (!syntheticEvent.__stopPropagation) {
+     triggerEventFlow(bubble, syntheticEvent);
+   }
+ }
+
+ function collectPaths(
+   targetElement: DOMElement,
+   container: Container,
+   eventType: string
+ ) {
+   const paths: Paths = { capture: [], bubble: [] };
+   while (targetElement && targetElement !== container) {
+     const elementProps = targetElement[elementPropsKey];
+     if (elementProps) {
+       const callbackNameList = getEventCallbackNameFromEventType(eventType);
+       if (callbackNameList) {
+         callbackNameList.forEach((callbackName, i) => {
+           const callback = elementProps[callbackName];
+           if (callback) {
+             if (i == 0) {
+               paths.capture.unshift(callback);
+             } else {
+               paths.bubble.push(callback);
+             }
+           }
+         });
+       }
+     }
+     targetElement = targetElement.parentNode as DOMElement;
+   }
+   return paths;
+ }
+
+ function getEventCallbackNameFromEventType(
+   eventType: string
+ ): string[] | undefined {
+   return {
+     click: ['onClickCapture', 'onClick']
+   }[eventType];
+ }
+
+ function createSyntheticEvent(e: Event) {
+   const syntheticEvent = e as SyntheticEvent;
+   syntheticEvent.__stopPropagation = false;
+   const originStopPropagation = e.stopPropagation;
+   syntheticEvent.stopPropagation = () => {
+     syntheticEvent.__stopPropagation = true;
+     if (originStopPropagation) {
+       originStopPropagation();
+     }
+   };
+   return syntheticEvent;
+ }
+
+ function triggerEventFlow(
+   paths: EventCallback[],
+   syntheticEvent: SyntheticEvent
+ ) {
+   for (let i = 0; i < paths.length; i++) {
+     const callback = paths[i];
+     callback.call(null, syntheticEvent);
+     if (syntheticEvent.__stopPropagation) {
+       break;
+     }
+   }
+ }
```

---

### 步骤 2:更新 hostConfig，接入 updateFiberProps

**路径:** `packages/react-dom/src/hostConfig.ts`

**修改内容:**
1. 导入 `HostComponent` workTag
2. 导入 `Props` 类型和 `DOMElement`、`updateFiberProps`
3. `createInstance` 补充 `props` 参数，创建 DOM 后立即调用 `updateFiberProps` 挂载初始 props
4. `commitUpdate` 中补全 `HostComponent` 分支，调用 `updateFiberProps` 更新 props 缓存

```diff
  import { FiberNode } from 'react-reconciler/src/fiber';
- import { HostText } from 'react-reconciler/src/workTags';
+ import { HostComponent, HostText } from 'react-reconciler/src/workTags';
+ import { Props } from 'shared/ReactTypes';
+ import { DOMElement, updateFiberProps } from './SyntheticEvent';

- export const createInstance = (type: string /*, props: any */): Instance => {
-   const element = document.createElement(type);
-   return element;
- };
+ export const createInstance = (type: string, props: Props): Instance => {
+   const element = document.createElement(type) as unknown;
+   updateFiberProps(element as DOMElement, props);
+   return element as DOMElement;
+ };

  export const commitUpdate = (fiber: FiberNode) => {
    switch (fiber.tag) {
-     // case HostComponent:
-     //   return updateFiberProps(fiber.stateNode, fiber.memoizedProps);
+     case HostComponent:
+       return updateFiberProps(fiber.stateNode, fiber.memoizedProps);

      case HostText:
        // ...
    }
  };
```

---

### 步骤 3:更新 completeWork，HostComponent 更新阶段只打 Update 标记

**路径:** `packages/react-reconciler/src/completeWork.ts`

**修改内容:**
1. `HostComponent` 更新分支不再直接调用 `updateFiberProps`
2. 改为调用 `markUpdate(wip)` 只打 `Update` flag，将真实 DOM 更新推迟到 commit 阶段
3. mount 分支补全 `createInstance(wip.type, newProps)`，传入 props

```diff
    case HostComponent:
      if (current !== null && wip.stateNode) {
-       // todo update
+       // update
+       markUpdate(wip);
      } else {
-       const instance = createInstance(wip.type);
+       const instance = createInstance(wip.type, newProps);
        appendAllChildren(instance, wip);
        wip.stateNode = instance;
      }
```

---

### 步骤 4:在 root.ts 中初始化事件系统

**路径:** `packages/react-dom/src/root.ts`

**修改内容:**
1. 导入 `initEvent`
2. 在 `render` 方法里调用 `initEvent(container, 'click')`，将 click 事件委托到根容器

```diff
+ import { initEvent } from './SyntheticEvent';

  export function createRoot(container: Container) {
    const root = createContainer(container);
    return {
      render(element: ReactElementType) {
+       initEvent(container, 'click');
        return updateContainer(element, root);
      }
    };
  }
```

---

### 步骤 5:更新 demo，验证事件系统

**路径:** `demos/test-fc/main.tsx`

**修改内容:**
1. 给根 `<div>` 添加 `onClick` 事件，点击后更新 `count` 状态，验证合成事件触发 + 状态更新的完整链路

```diff
- return <div>{jsx}</div>;
+ return <div onClick={() => setCount(count + 1)}>{jsx}</div>;
```

---

## 第三部分:主要知识点

（待补充）

---

## 第四部分:个人 Q&A

（待补充）
